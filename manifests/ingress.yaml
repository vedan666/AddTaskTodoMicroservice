apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: add-task-api-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: add-task-api.memesforlife.sbs
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: todo-ui
            port: 
              number: 80
